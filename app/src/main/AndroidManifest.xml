<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplicationDemo.NoActionBar">
        <activity
            android:name=".TaskListActivity"
            android:exported="false" />
        <activity
            android:name=".Domains.Projects.Activities.AddMemberActivity"
            android:exported="false" />
        <activity
            android:name=".Domains.Projects.Activities.item_member"
            android:exported="false" />
        <activity
            android:name=".Domains.Projects.Activities.ProjectDetailActivity"
            android:exported="false" />
        <activity
            android:name=".Domains.Projects.Activities.ProjectDetailActivity"
            android:exported="false" /> <!-- Home Activity - Màn hình chính sau đăng nhập -->
        <activity
            android:name=".HomeActivity"
            android:exported="false" /> <!-- Create Project Activity - Màn hình tạo project mới -->
        <activity
            android:name=".CreateProjectActivity"
            android:exported="false" /> <!-- Register Activity - Màn hình đăng ký -->
        <activity
            android:name=".auth.RegisterActivity"
            android:exported="false" /> <!-- Login Activity - Launcher, màn hình đầu tiên -->
        <activity
            android:name=".auth.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>